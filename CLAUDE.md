# 프로젝트 안내

고등학생용 어휘 테스트 웹앱입니다.
빌드 도구 없이 `index.html` 을 열면 그대로 실행되는 순수 HTML/CSS/JS 구조를 유지합니다.

## 디자인 톤 (화면을 추가할 때도 계속 유지할 것)

이 앱의 시각 톤은 **연보라(라벤더) 계열 + 모바일에서 읽기 편한 큰 글씨** 입니다.
새 화면·새 요소를 만들 때 아래를 지킵니다.

1. **색은 반드시 `style.css` 의 `:root` 변수에서 가져다 씁니다.**
   `var(--primary)`, `var(--card)`, `var(--muted)` 처럼 쓰고,
   `#7c3aed` 같은 색 코드를 컴포넌트에 직접 적지 않습니다.
   새 색이 꼭 필요하면 `:root`(밝은 화면), `@media (prefers-color-scheme: dark)`,
   `:root[data-theme="dark"]` **세 곳 모두**에 변수를 추가합니다.
2. **강조색은 연보라 하나만** 씁니다. 정답 초록·오답 빨강은 채점 표시 전용이며
   강조색으로 쓰지 않습니다.
   틀린 문제 해설의 `내 답` 은 회색(`--answer-mine`), `정답` 은 선명한 분홍(`--answer-right`) 입니다.
   틀린 답을 붉게 강조하지 않고, 학생 눈이 정답으로 가게 두는 것이 목적입니다.
3. **글씨체는 두 가지만** 씁니다. 제목·문제 어휘처럼 큰 글씨는 `--font-title`
   (카페24 고운밤), 보기·해설 같은 본문은 `--font-body` (나눔스퀘어).
   두 글씨체 모두 **한자가 없어서** `@font-face` 에 `unicode-range` 로 한글·영문만
   맡기고, 한자는 휴대폰 기본 글씨체가 그리게 둡니다. 이 범위를 지우지 마세요.
   굵은 글씨(700·800)를 쓰는 파일이 없으므로 큰 글씨에는 `font-weight: 400` 을 씁니다.
   고운밤은 굵은 파일이 없어서 `-webkit-text-stroke` 로 획을 덧그려 굵게 보이게 합니다.
   굵기는 `:root` 의 `--title-bold` 한 곳에서 정합니다.
   **한자에는 덧그리기를 하지 않습니다.** 획이 촘촘해서 뭉개집니다. (`.wrong-hanja`)
4. **글씨 크기 기준** (휴대폰에서 편히 읽히는 크기):
   본문 16px / 보기 14px / 입력칸·버튼 17px / 화면 제목 30px / 문제 어휘 40px.
   보기는 글씨체에 중간 굵기가 없어 `-webkit-text-stroke: 0.3px` 로 반 단계만 두껍게 합니다.
   (고른 보기와 번호 동그라미는 제외)
   이보다 작게 줄이지 않습니다. 틀린 문제 해설 카드는 `내 답`·`정답`·해설·예문을
   모두 15px 한 크기로 맞추고, 해설만 글자색을 진하게 해서 먼저 눈에 들어오게 합니다.
   작은 화면(380px 이하)용 크기는 파일 맨 아래에 모아둡니다.
5. **모서리는 둥글게, 그림자는 옅게.** 카드 `border-radius: 16~18px`,
   버튼·보기 `14px`, 그림자는 `var(--shadow)` 만 사용합니다.
6. **터치 영역을 넉넉히.** 누를 수 있는 요소의 세로 크기는 48px 이상으로 잡습니다.
7. **첫 화면(회차 고르기)만 로고와 같은 진한 보라 바탕**(`--brand-deep`)입니다.
   `html` 에 `on-rounds` 를 붙여 칠하고, 휴대폰 맨 위 띠 색도 같이 바꿉니다. (`app.js` 의 `paintBackground`)
   `html` 과 `body` 두 곳에 모두 칠해야 화면 끝까지 색이 채워집니다.
8. **밝은 화면과 어두운 화면을 모두 확인합니다.** 어느 한쪽에서만 보이는 색을 만들지 않습니다.
9. **가로 스크롤이 생기지 않게** 합니다. 긴 글은 줄바꿈되게 두고, 폭을 고정하지 않습니다.
10. **해설과 예문은 나눠서 보여 줍니다.** 해설 문자열의 `예)` `유래)` 뒤는
   별도의 예문 상자로 그립니다. (`app.js` 의 `splitExplanation`)
11. **숨김은 `hidden` 속성으로** 하고, 컴포넌트에 `display` 를 줄 때는
   `[hidden] { display: none !important; }` 규칙에 가려지지 않는지 확인합니다.

## 파일 역할

| 파일 | 역할 |
|---|---|
| `questions.js` | 회차와 문제 데이터. **사용자(선생님)가 직접 고치는 유일한 파일** |
|  | 출제 수는 `QUIZ_LENGTH`, 한자성어 수는 `IDIOM_COUNT`, 재시험 기준은 `RETEST_MAX`, 한자성어 표시는 `type: "한자성어"` |
| `index.html` | 화면 구조 |
| `style.css` | 색·글씨·여백 |
| `app.js` | 회차 선택, 출제, 채점, 화면 전환 (학생용) |
| `teacher.html` `teacher.js` | 선생님용 화면 (학생용과 완전히 분리) |
| `storage.js` | 점수 기록 저장 (기기 저장 + 수파베이스 전송) |
| `fonts/` | 앱이 쓰는 글씨체 (`원본/` 은 받은 파일 그대로 보관) |
| `logo/` | 학원 로고 (보라색·흰색, 가로·세로·마크·글씨) |
| `manifest.json` `icon-*.png` | 바탕화면에 앱처럼 놓기 위한 설정과 아이콘 |
| `samples/` | 참고용 예시 문제 파일 |

## 회차를 추가한 뒤

`questions.js` 에 회차를 추가했으면 **수파베이스 문제 은행에도 바로 올립니다.**
- 수파베이스 커넥터를 쓸 수 있으면 `sync_questions` 로 직접 올립니다.
- 쓸 수 없으면 선생님용 화면의 `문제 은행에 올리기` 버튼으로 올리도록 안내합니다.

## 파일을 고친 뒤

`index.html`, `check.html` 안의 `?v=날짜` 표시를 새 값으로 바꿉니다.
바꾸지 않으면 학생 휴대폰이 예전 파일을 계속 씁니다.

## 사용자에 대해

이 저장소의 사용자는 코딩을 하지 않습니다.
- 코드 주석과 문서는 **한국어로, 전문 용어 없이** 씁니다.
- `questions.js` 는 비개발자가 메모장으로 열어 고칠 수 있어야 합니다.
  구조를 바꿀 때는 파일 맨 위 주석의 설명도 함께 갱신합니다.
- 문제 파일에 형식 오류가 있어도 앱이 그냥 멈추지 않고,
  **어느 회차 몇 번째 문제가 잘못됐는지** 화면에 안내해야 합니다. (`app.js` 의 `validateRounds`)

## 숙제 회차

**회차는 모두 보여 주되, 선생님이 정한 회차 하나만 누를 수 있습니다.**
- 회차 버튼은 세 칸씩 놓고, 번호를 크게 주제를 작게 씁니다. (`round-btn`)
- 열려 있지 않은 회차는 테두리만 남기고 누를 수 없게 합니다. (`is-locked`)
  학생이 전체 진도를 눈으로 볼 수 있게 하려는 것입니다.
- 회차는 수파베이스 `app_settings` 의 `homework_round` 에 저장합니다.
- 학생 앱은 이 값만 읽을 수 있고, 정하는 것은 비밀번호가 필요한 `set_homework_round` 함수로만 합니다.
- 값이 비어 있으면 모든 회차가 열립니다. 이름이 맞는 회차가 없어도 모두 열어
  학생이 아예 못 푸는 일이 없게 합니다.

## 누가 시험을 볼 수 있나

주소를 아는 사람이 아무나 풀지 못하게, **재원생 명단에 있는 학생만** 시작할 수 있습니다.
- 시작 전에 `check_enrolled(이름, 뒷4자리)` 로 서버에 물어봅니다. (`storage.js` 의 `isEnrolled`)
- 명단은 앱으로 내려보내지 않습니다. **맞다/아니다만** 주고받습니다.
- 확인이 안 되면(인터넷 끊김 포함) 시작하지 않습니다. 통과시키지 마세요.
- 앱에서만 막지 않습니다. `quiz_attempts` 넣기 정책에서 **서버도 같은 기준으로** 막습니다.
- 기준은 아래 '같은 학생 판단 기준'과 같습니다. 학교는 쓰지 않습니다.

선생님 계정은 명단(`students`)에서 **메모(`memo`)가 `선생님` 으로 시작하는 줄** 입니다.
- 시험은 볼 수 있지만 **통계에는 넣지 않습니다.** (`is_teacher` 로 걸러냅니다)
  재원생 인원, 회차별 평균, 학생별 누적, 어휘별 정답률, 기록 목록·CSV 모두 해당됩니다.
- `sync_students` 로 시트와 맞출 때도 내리지 않고 그대로 둡니다.
- 통계를 새로 만들 때는 `is_teacher(이름, 뒷4자리)` 로 거르는 것을 잊지 마세요.

## 학생 개인정보

기록에는 학생 이름·학교·학부모님 전화번호 뒷자리가 들어갑니다.
- **학생 화면에는 선생님용 요소를 두지 않습니다.** 버튼도 링크도 코드도 넣지 않습니다.
- 학생에게는 **자기 기록만** 보여 줍니다. 전체 명단은 선생님용 화면(teacher.html)에서만 봅니다.
- 수파베이스 표는 넣기(insert)만 열고 읽기(select)는 닫아 둡니다.
- 재원생 명단(`students`)에는 연락처가 들어갑니다. 이 표도 읽기를 완전히 닫아 두고,
  `admin_students` 함수로만 꺼냅니다.
- 같은 학생 판단 기준은 **이름(띄어쓰기 무시) + 학부모 연락처 뒷 4자리** 입니다.
  학교는 표기가 달라질 수 있어 기준으로 쓰지 않습니다.
- 재원생 명단이 시트와 달라지면 `sync_students` 함수로 맞춥니다.
  **학생 줄을 지우지 않습니다.** `active = false` 로 내리고 시험 기록은 보존합니다.
- 전체 기록은 `admin_attempts` 함수로만 읽습니다.
  **선생님 비밀번호를 앱 파일에 적지 않습니다.** 서버에서 확인하게 합니다.
  (앱에 적으면 학생이 파일을 열어 볼 수 있습니다)
- `localStorage` 를 쓸 수 없는 브라우저에서도 앱이 멈추지 않아야 합니다.
