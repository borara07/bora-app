# 프로젝트 안내

고등학생용 어휘 테스트 웹앱입니다.
빌드 도구 없이 `index.html` 을 열면 그대로 실행되는 순수 HTML/CSS/JS 구조를 유지합니다.

## 디자인 톤 (화면을 추가할 때도 계속 유지할 것)

이 앱의 시각 톤은 **연보라(라벤더) 계열 + 모바일에서 읽기 편한 큰 글씨** 입니다.
새 화면·새 요소를 만들 때 아래를 지킵니다.

1. **색은 반드시 `style.css` 의 `:root` 변수에서 가져다 씁니다.**
   `var(--primary)`, `var(--card)`, `var(--muted)` 처럼 쓰고,
   `#7c3aed` 같은 색 코드를 컴포넌트에 직접 적지 않습니다.
   새 색이 꼭 필요하면 `:root`(밝은 화면), `@media (prefers-color-scheme: dark)`,
   `:root[data-theme="dark"]` **세 곳 모두**에 변수를 추가합니다.
2. **강조색은 연보라 하나만** 씁니다. 정답 초록·오답 빨강은 채점 표시 전용이며
   강조색으로 쓰지 않습니다.
   틀린 문제 해설의 `내 답` 은 회색(`--answer-mine`), `정답` 은 선명한 분홍(`--answer-right`) 입니다.
   틀린 답을 붉게 강조하지 않고, 학생 눈이 정답으로 가게 두는 것이 목적입니다.
3. **글씨체는 두 가지만** 씁니다. 제목·문제 어휘처럼 큰 글씨는 `--font-title`
   (카페24 고운밤), 보기·해설 같은 본문은 `--font-body` (나눔스퀘어).
   두 글씨체 모두 **한자가 없어서** `@font-face` 에 `unicode-range` 로 한글·영문만
   맡기고, 한자는 휴대폰 기본 글씨체가 그리게 둡니다. 이 범위를 지우지 마세요.
   **드문 한글도 조심합니다.** 두 글씨체는 자주 쓰는 2,700자만 그릴 수 있는데,
   원본 파일에는 나머지 8,700자가 *빈 그림*으로 들어 있었습니다. 그러면 글자가
   자리만 차지하고 안 보입니다 (`앋 · 핟 · 옫 · 밷` 같은 발음 표기 글자).
   그래서 `fonts/*.woff2` 는 **그림이 있는 글자만 남겨** 다시 만들었습니다.
   없는 글자는 휴대폰 기본 글씨체가 그립니다. 글씨체를 새로 넣을 때도 같은 손질을 하세요.
   글씨체 파일을 바꾸면 `style.css` 의 `fonts/....woff2?v=` 표시도 함께 바꿔야
   휴대폰이 새 파일을 받습니다.
   굵은 글씨(700·800)를 쓰는 파일이 없으므로 큰 글씨에는 `font-weight: 400` 을 씁니다.
   고운밤은 굵은 파일이 없어서 `-webkit-text-stroke` 로 획을 덧그려 굵게 보이게 합니다.
   굵기는 `:root` 의 `--title-bold` 한 곳에서 정합니다.
   **한자에는 덧그리기를 하지 않습니다.** 획이 촘촘해서 뭉개집니다. (`.wrong-hanja`)
4. **글씨 크기 기준** (휴대폰에서 편히 읽히는 크기):
   본문 16px / 보기 14px / 입력칸·버튼 17px / 화면 제목 30px / 문제 어휘 40px.
   보기는 글씨체에 중간 굵기가 없어 `-webkit-text-stroke: 0.3px` 로 반 단계만 두껍게 합니다.
   (고른 보기와 번호 동그라미는 제외)
   이보다 작게 줄이지 않습니다. 틀린 문제 해설 카드는 `내 답`·`정답`·해설·예문을
   모두 15px 한 크기로 맞추고, 해설만 글자색을 진하게 해서 먼저 눈에 들어오게 합니다.
   `내 답`·`정답` 이름표는 값 위에 작게 올려, 카드 안의 글이 모두 왼쪽 한 줄로 정렬되게 합니다.
   작은 화면(380px 이하)용 크기는 파일 맨 아래에 모아둡니다.
5. **모서리는 둥글게, 그림자는 옅게.** 카드 `border-radius: 16~18px`,
   버튼·보기 `14px`, 그림자는 `var(--shadow)` 만 사용합니다.
6. **터치 영역을 넉넉히.** 누를 수 있는 요소의 세로 크기는 48px 이상으로 잡습니다.
7. **시작 화면 두 개(이름 입력·회차 고르기)는 로고와 같은 진한 보라 바탕**(`--brand-deep`)입니다.
   이 두 화면에서는 **흰 상자를 쓰지 않습니다.**
   입력칸도 버튼도 모두 반투명 흰 테두리로 같은 모양입니다.
   **흰색으로 채우는 것은 '열린 회차' 하나뿐입니다.**
   `html` 에 `on-rounds` 를 붙여 칠하고, 휴대폰 맨 위 띠 색도 같이 바꿉니다. (`app.js` 의 `paintBackground`)
   `html` 과 `body` 두 곳에 모두 칠해야 화면 끝까지 색이 채워집니다.
8. **밝은 화면과 어두운 화면을 모두 확인합니다.** 어느 한쪽에서만 보이는 색을 만들지 않습니다.
9. **가로 스크롤이 생기지 않게** 합니다. 긴 글은 줄바꿈되게 두고, 폭을 고정하지 않습니다.
10. **해설과 예문은 나눠서 보여 줍니다.** 해설 문자열의 `예)` `유래)` 뒤는
   별도의 예문 상자로 그립니다. (`app.js` 의 `splitExplanation`)
11. **숨김은 `hidden` 속성으로** 하고, 컴포넌트에 `display` 를 줄 때는
   `[hidden] { display: none !important; }` 규칙에 가려지지 않는지 확인합니다.

## 파일 역할

| 파일 | 역할 |
|---|---|
| `questions.js` | 회차와 문제 데이터. **사용자(선생님)가 직접 고치는 유일한 파일** |
|  | 회차마다 `group` 으로 반을 적습니다 (고등부 / 중등부) |
|  | 출제 수는 `QUIZ_LENGTH`, 한자성어 수는 `IDIOM_COUNT`, 재시험 기준은 `RETEST_MAX`, 한자성어 표시는 `type: "한자성어"` |
| `questions-grammar.js` | 문법 회차와 문제 데이터 (어휘와 같은 모양, 목록 이름만 `GRAMMAR_ROUNDS`) |
|  | 맞춤법 둘 중 고르기 · OX · 5지선다 세 가지를 낼 수 있습니다 |
| `exams.js` | 모의고사 회차와 정답·배점·영역·등급컷 (**선생님이 직접 고치는 파일**) |
| `index.html` `grammar.html` | 학생 화면 구조 (어휘 / 문법). 화면과 코드는 똑같이 쓰고 과목만 다릅니다 |
| `exam.html` `exam.js` | 모의고사 학생 화면 (어휘·문법과 푸는 방식이 달라 따로 있습니다) |
| `style.css` | 색·글씨·여백 |
| `app.js` | 회차 선택, 출제, 채점, 화면 전환 (학생용, 두 과목 공용) |
| `teacher.html` `teacher.js` | 선생님용 화면 (학생용과 완전히 분리) |
| `storage.js` | 점수 기록 저장 (기기 저장 + 수파베이스 전송) |
| `fonts/` | 앱이 쓰는 글씨체 (`원본/` 은 받은 파일 그대로 보관) |
| `logo/` | 학원 로고 (보라색·흰색, 가로·세로·마크·글씨) |
| `manifest.json` `icon-*.png` | 바탕화면에 앱처럼 놓기 위한 설정과 아이콘 (학생용) |
| `teacher-manifest.json` `teacher-*.png` | 선생님용 설치 설정과 아이콘 |
| `grammar-manifest.json` `grammar-*.png` | 문법 앱 설치 설정과 아이콘 |
| `exam-manifest.json` `exam-*.png` | 모의고사 앱 설치 설정과 아이콘 (연보라 바탕에 흰 로고) |
|  | 학생용은 진보라 바탕에 흰 로고, 선생님용은 흰 바탕에 보라 테두리입니다 |
| `samples/` | 참고용 예시 문제 파일 |
| `모의고사/` | 모의고사 성적표 엑셀 (앱을 쓰기 전에 쓰던 것. 인쇄용으로 계속 씁니다) |

## 회차를 추가한 뒤 — 먼저 검토를 받습니다

**새 회차는 선생님이 검토하기 전에는 문제 은행에 올리지 않습니다.**
선생님이 직접 만든 문제가 아니라 자료에서 옮겨 온 문제이므로,
정답과 해설을 사람이 한 번 보고 넘어가야 합니다.

순서는 이렇습니다.

1. `questions.js` · `questions-grammar.js` 에 회차를 넣습니다.
2. **검토판을 만들어 드리고 확인을 받습니다.**
   앱에 화면을 만들지 않고, 저장소에 파일로 남기지도 않습니다.
   **대화창에서 아티팩트(웹 화면)로 만들어 드립니다.** 전에 01~07회를 검토한 방식과 같습니다.
   - 회차 단추를 눌러 회차를 바꿉니다.
   - 문제마다 물음·제시문·보기(정답에 초록 ✓)·해설을 모두 보여 줍니다.
   - 빼고 싶은 문제의 **네모를 누르면** 아래 띠에 번호가 모이고, `복사` 로 가져가
     대화창에 붙여 넣어 주십니다. 실제로 빼는 것은 제가 합니다.
   - 고른 것은 그 기기에 남아 도중에 닫아도 됩니다. 번호는 회차마다 따로 셉니다.
3. 선생님이 **괜찮다고 하시면** 그때 문제 은행에 올립니다.
   - 수파베이스 커넥터를 쓸 수 있으면 `sync_questions` 로 직접 올립니다.
   - 쓸 수 없으면 선생님용 화면의 `문제 은행에 올리기` 버튼으로 올리도록 안내합니다.
4. 이번 주 시험으로 여는 것은 언제나 선생님이 정합니다.

새 회차는 선생님이 열어 주기 전까지 학생에게 잠겨 있지만,
`homework_round:문법:고등부` 처럼 **빈 값으로 두면 그 반은 모든 회차가 열립니다.**
검토 전 회차가 여기에 딸려 나가지 않는지 확인해 주세요.

## 파일을 고친 뒤

`index.html`, `check.html` 안의 `?v=날짜` 표시를 새 값으로 바꿉니다.
바꾸지 않으면 학생 휴대폰이 예전 파일을 계속 씁니다.

## 사용자에 대해

이 저장소의 사용자는 코딩을 하지 않습니다.
- 코드 주석과 문서는 **한국어로, 전문 용어 없이** 씁니다.
- `questions.js` 는 비개발자가 메모장으로 열어 고칠 수 있어야 합니다.
  구조를 바꿀 때는 파일 맨 위 주석의 설명도 함께 갱신합니다.
- 문제 파일에 형식 오류가 있어도 앱이 그냥 멈추지 않고,
  **어느 회차 몇 번째 문제가 잘못됐는지** 화면에 안내해야 합니다. (`app.js` 의 `validateRounds`)

## 과목 (어휘 / 문법 / 모의고사)

**어휘와 문법은 화면·코드를 그대로 함께 쓰고, 주소만 다릅니다.**
- `index.html` 은 어휘, `grammar.html` 은 문법입니다.
  두 파일 모두 같은 `app.js`·`style.css`·`storage.js` 를 씁니다.
- 어느 과목인지는 각 html 맨 아래 `var APP_SUBJECT = "어휘";` 한 줄로 정합니다.
- 회차에 `mix: { 단순: 12, 객관식: 3 }` 을 적으면 한 번 시험에 나올 구성을 정할 수 있습니다.
  (단순 = OX·보기 2개 / 객관식 = 보기 3개 이상)
- **한 회차 안에서 문제 이름이 겹치면** 문제에 `name` 을 적어 따로 지어 줍니다.
  선생님 화면의 '문제별 정답률' 은 제시문(없으면 정답)으로 문제를 구분하는데,
  이름이 같으면 두 문제가 한 칸을 나눠 쓰고 문제 은행에도 하나만 올라갑니다.
  `name` 은 화면에 보이지 않습니다. (`app.js` 의 `toQuizItems`, `teacher.js` 의 `allQuestions`)
- **비슷한 문제가 한 시험에 몰리지 않게** 갈래마다 최대 개수를 정할 수 있습니다.
  문제에 `kind: "동사형용사"` 를 적고 회차에 `limit: { 동사형용사: 5 }` 를 적습니다.
  갈래 이름은 마음대로 지어도 되고, 안 적은 문제는 세지 않습니다. (`app.js` 의 `pickUpTo`)
  limit 때문에 모자라면 다른 문제로 채우므로 시험 문제 수는 줄지 않습니다.
- 문제를 만들 때 쓰는 자료는 `문법자료/` 에 모아 둡니다.
- 문법 문제는 보기를 **2개에서 5개까지** 넣을 수 있습니다.
  `ox: true` 라고 적으면 앱이 O·X 보기를 자동으로 넣고, 순서도 섞지 않습니다.
- 문법 문제에는 `ask`(물음)와 `sentence`(제시문)를 씁니다.
  어휘처럼 `word`·`hanja` 를 써도 됩니다.
- 이번 주 시험 회차는 **과목마다 따로** 정합니다.
  어휘는 **반**으로, 문법은 **학년**으로 나눕니다.
  `homework_round:고등부`(어휘), `homework_round:문법:고1`(문법)
  학년은 명단 메모 앞머리에서 읽습니다 (`고1 금 · 26기` → 고1). `guess_grade` 함수입니다.
  문법 회차는 학년으로 열고 닫으므로 회차의 `group` 으로 가르지 않습니다.
- 기록에는 문법 회차 이름 앞에 `[문법]` 이 붙어 저장됩니다.
  선생님 화면에서 어휘와 섞이지 않게 하려는 것입니다.

## 모의고사 (exam.html)

**매주 보는 국어 모의고사를 학생이 폰으로 채점받는 화면입니다.**
어휘·문법과 달리 앱이 문제를 내지 않습니다. 시험은 종이로 보고,
학생은 **자기 OMR에 칠한 번호를 그대로 눌러 넣기만** 합니다.

- 화면 순서는 **이름 입력 → 회차 고르기 → 선택과목 고르기 → 45칸 입력 → 성적표** 입니다.
- 회차 단추에는 **학년을 크게, 모의고사 이름을 작게** 씁니다 (`고3` / `30차 심화모의고사`).
  이름이 길어서 세 칸으로 나누지 않고 **한 줄에 하나씩** 넓게 놓습니다. (`round-list is-wide`)
  모의고사 이름은 회차마다 그때그때 정하므로 `exams.js` 의 `title` 에 적습니다.
- 45문항을 한 화면에 줄줄이 놓고, 한 줄에 ①~⑤ 다섯 칸을 둡니다. (`sheet-row`)
  같은 번호를 다시 누르면 지워집니다. 칸 하나의 세로 크기는 48px 이상으로 둡니다.
- **45번까지 다 누르지 않아도 바로 채점됩니다.** 시간 안에 못 푼 학생이 있기 때문입니다.
  안 누른 문항은 틀린 것이 아니라 **'못 푼 문제'** 로 따로 표시하고 따로 셉니다.
  결과에 `틀린 문항 13개 · 못 푼 문항 7개` 처럼 나오고, 기록에도 `blank: true` 로 남습니다.
  못 푼 문항의 `내 답` 은 `표시 안 함` 입니다. 점수에는 0점으로 들어갑니다.
- **선택과목(화법과 작문 / 언어와 매체)에 따라 35~45번 정답표가 다릅니다.**
  1~34번은 공통입니다. 몇 번까지가 공통인지는 `EXAM_COMMON_COUNT` 로 정합니다.
- 채점은 **배점(2점·3점)** 으로 하고, 영역 표를 보고 **영역별 성취도**를 냅니다.
  등급은 회차마다 적어 둔 **등급컷** 으로 정합니다.
- 회차는 **학년**으로 엽니다. (`homework_round:모의고사:고3`)
  문법과 같은 방식이라 수파베이스는 고칠 것이 없습니다.
- 기록에는 회차 이름 앞에 `[모의고사]`, 뒤에 `· 화작` 또는 `· 언매` 가 붙습니다.
  점수(100점 만점)는 정답률 자리에 그대로 들어갑니다. 배점 합이 100점이라 같은 값입니다.

### exams.js 에 회차를 넣을 때

회차마다 적는 것은 여섯 가지입니다 — `grade`(학년) · `title`(모의고사 이름) ·
`date`(시행일) · `cuts`(등급컷) · `answers`(정답) · `points`(배점) · `areas`(영역).

엑셀 `모의고사/모의고사-성적표-*.xlsx` 의 `회차설정` 시트와 같은 내용입니다.
정답과 배점은 **1번부터 차례로 한 글자씩** 이어서 적습니다.

앱이 열릴 때 아래를 스스로 검사해서, 틀리면 **어디가 잘못됐는지 화면에 적어 줍니다.**
(`exam.js` 의 `validateExams`. 엑셀의 `확인` 칸과 같은 검사입니다)

- 정답·배점 개수가 맞는지, 정답이 1~5 인지
- 선택과목마다 **배점 합이 100점** 인지
- 영역 표가 **45문항을 빠짐없이 한 번씩** 덮는지 (빠진 문항·겹친 문항을 번호로 알려 줍니다)
- 등급컷이 네 개이고 높은 순서인지

이 검사가 지난 수기 엑셀에서 났던 오류(갈래복합 성취도 118%)를 막습니다.

## 반 (고등부 / 중등부)

**학생마다 반이 있고, 반마다 다른 어휘 시험을 봅니다.**
- 학생의 반은 재원생 명단(`students.grade_group`)에 있습니다.
  비어 있으면 메모 첫 글자로 짐작합니다 (`중` 으로 시작하면 중등부, 나머지는 고등부).
- 회차마다 `group: "고등부"` 를 적습니다. 안 적으면 고등부로 봅니다.
- 화면 순서는 **이름 입력 → 내 반 회차 고르기 → 시험** 입니다.
  이름을 확인해야 그 학생의 반을 알 수 있어서 이름을 먼저 받습니다.
- 재원생 확인은 `check_enrolled_group` 으로 합니다. 맞다/아니다와 **그 학생의 반만** 돌려줍니다.
  명단은 여전히 앱으로 내려보내지 않습니다.
- 선생님 화면에서는 반을 골라 숙제 회차를 정하고, 통계도 반별로 볼 수 있습니다.
- **재원생 명단은 학년마다 상자를 따로 만들어 보여 줍니다.** 상자는 `teacher.js` 의
  `drawRosterByGrade` 가 명단을 보고 그때그때 만듭니다. 학년 순서는 `GRADE_ORDER` 로 정합니다.
- 반을 늘리려면 `questions.js` 의 회차에 새 반 이름을 적고,
  `teacher.html` 의 `stat-group` 과 `teacher.js` 의 `GROUPS` 에 줄을 하나씩 더합니다.
- 선생님 화면의 반/학년 칸은 `teacher.js` 의 `GRADES`·`GROUPS` 로 채웁니다.
  문법 목록 끝에는 `고등부`·`중등부` 도 넣어 두었습니다.
  명단 메모에 학년이 없는 사람(선생님 계정 등)은 반 이름으로 회차를 찾기 때문입니다.

## 숙제 회차

**회차는 모두 보여 주되, 선생님이 정한 회차만 누를 수 있습니다. 여러 개를 정할 수 있습니다.**
- 회차 버튼은 세 칸씩 놓고, 번호를 크게 주제를 작게 씁니다. (`round-btn`)
- 열려 있지 않은 회차는 테두리만 남기고 누를 수 없게 합니다. (`is-locked`)
  학생이 전체 진도를 눈으로 볼 수 있게 하려는 것입니다.
- 회차는 어휘는 반마다, 문법·모의고사는 학년마다 따로 정합니다. 수파베이스 `app_settings` 의
  `homework_round:고등부`, `homework_round:중등부`,
  `homework_round:문법:고1` … `homework_round:문법:중1`,
  `homework_round:모의고사:고3` 에 저장합니다.
- 학생 앱은 이 값만 읽을 수 있고, 정하는 것은 비밀번호가 필요한 `set_homework_round` 함수로만 합니다.
- **정한 적이 없는 반·학년은 아무 회차도 열리지 않습니다.** 학생에게는
  '이번 주 시험이 아직 정해지지 않았습니다' 안내가 뜹니다.
  선생님이 정하지 않은 학년이 시험을 보는 일이 없게 하려는 것입니다.
- 값을 **빈 글자로 정하면**(선생님 화면의 '모든 회차 열기') 그때는 모두 열립니다.
  '한 번도 정한 적 없음'과 '일부러 모두 열어 둠'을 이렇게 가릅니다.
  (`storage.js` 의 `homeworkRound` 가 `set` 으로 알려 줍니다)
- 정한 회차 이름이 목록에 없으면 역시 아무것도 열지 않고 안내를 띄웁니다.
- **여러 회차를 한 번에 열 수 있습니다.** 선생님 화면의 `이번 주 시험` 은 네모를 눌러
  고르는 목록(`pick-list`)이고, 고른 회차 이름을 `|` 로 이어 붙여 한 칸에 저장합니다.
  (`01회 맞춤법 1|02회 맞춤법 2`) 회차 이름에는 `|` 를 쓰지 않습니다.
  맨 위 '모든 회차 열기' 를 켜면 빈 글자로 저장되어 전부 열립니다.
  읽는 곳은 `app.js` 의 `openTitles`, 쓰는 곳은 `teacher.js` 의 `pickedRounds` 입니다.
  값이 하나뿐인 예전 저장값도 그대로 읽힙니다.

## 선생님 화면은 세 과목을 한 곳에서 관리합니다

**학생 앱은 과목마다 따로지만(어휘·문법·모의고사 주소가 각각), 선생님 화면은 하나입니다.**
- `통계에서 볼 과목` 고르개(`stat-subject`)에서 전체 / 어휘 / 문법 / 모의고사를 고릅니다.
  고른 과목만 아래 **모든 표와 CSV**에 나옵니다. (`teacher.js` 의 `inSubject`)
- 과목은 회차 이름 앞머리로 가립니다. `[모의고사]` · `[문법]` · (아무것도 없으면) 어휘.
  (`subjectOf`) 새 과목을 만들면 저장할 때 앞머리를 붙이고 이 함수에 한 줄 더합니다.
- 과목을 고르면 회차 이름의 앞머리는 떼고 보여 줍니다. (`roundName`)
- `아직 안 본 학생` 도 **그 과목을** 안 본 학생이 됩니다. 주차 고르개의 횟수도 그 과목만 셉니다.
- 모의고사는 정답률이 아니라 100점 만점 점수라서, 요약에 `평균 점수` 로 적습니다.

## 선생님 화면의 통계는 '한 주' 가 기본입니다

**매주 시험을 보므로, 누적을 보여 주면 이번 주에 누가 안 봤는지 알 수 없습니다.**
- `통계에서 볼 기간` 고르개(`stat-period`)에서 주를 고릅니다. 한 주는 **월요일~일요일** 입니다.
- 고른 주의 기록만 아래 **모든 표**에 나옵니다.
  재원생 응시 현황 · 회차별 평균 · 학생별 · 기록 · CSV 내려받기 모두 같습니다.
- 처음 들어오면 **기록이 있는 가장 최근 주**가 골라져 있습니다. `전체 (누적)` 도 고를 수 있습니다.
- 고르개에는 그 주에 본 횟수가 같이 적혀 있어 어느 주가 비었는지 바로 보입니다.
- 명단(`admin_students`)이 주는 응시 수·평균은 '지금까지 전부' 라서 쓰지 않고,
  고른 주의 기록으로 다시 셉니다. (`teacher.js` 의 `rosterForPeriod`)
  그래서 `아직 안 본 학생` 은 **그 주에** 안 본 학생입니다.
- 주를 계산하는 곳은 `mondayOf` 한 곳뿐입니다. 주 시작 요일을 바꾸려면 여기만 고칩니다.

## 누가 시험을 볼 수 있나

주소를 아는 사람이 아무나 풀지 못하게, **재원생 명단에 있는 학생만** 시작할 수 있습니다.
- 시작 전에 `check_enrolled(이름, 뒷4자리)` 로 서버에 물어봅니다. (`storage.js` 의 `isEnrolled`)
- 명단은 앱으로 내려보내지 않습니다. **맞다/아니다만** 주고받습니다.
- 확인이 안 되면(인터넷 끊김 포함) 시작하지 않습니다. 통과시키지 마세요.
- 앱에서만 막지 않습니다. `quiz_attempts` 넣기 정책에서 **서버도 같은 기준으로** 막습니다.
- 기준은 아래 '같은 학생 판단 기준'과 같습니다. 학교는 쓰지 않습니다.

선생님 계정은 명단(`students`)에서 **메모(`memo`)가 `선생님` 으로 시작하는 줄** 입니다.
- 시험은 볼 수 있지만 **통계에는 넣지 않습니다.** (`is_teacher` 로 걸러냅니다)
  재원생 인원, 회차별 평균, 학생별 누적, 어휘별 정답률, 기록 목록·CSV 모두 해당됩니다.
- `sync_students` 로 시트와 맞출 때도 내리지 않고 그대로 둡니다.
- 통계를 새로 만들 때는 `is_teacher(이름, 뒷4자리)` 로 거르는 것을 잊지 마세요.

## 선생님 권한 (원장 / 다른 선생님)

**선생님 비밀번호는 두 가지입니다.** `admin_secret` 표의 `role` 칸으로 가릅니다.
- `admin` — 원장 선생님. 모든 것 (이번 주 회차 정하기, 문제 은행 올리기, 재원생 명단)
- `viewer` — 다른 선생님. **학생 기록 보기와 CSV 내려받기**까지 됩니다.
- 어느 권한인지는 `teacher_role(비밀번호)` 로 묻습니다. 틀리면 빈 글자를 돌려줍니다.
- **화면에서 감추는 것으로 끝내지 않습니다.** 서버 함수에서도 같은 기준으로 막습니다.
  `admin_students` · `set_homework_round` · `sync_questions` ·
  `admin_preview_students` · `admin_sync_students` 는 `role = 'admin'` 이어야 합니다.
- 새 기능을 만들 때 학생 개인정보나 설정을 건드리면 **원장 전용으로 두는 것이 기본**입니다.

## 학생 개인정보

기록에는 학생 이름·학교·학부모님 전화번호 뒷자리가 들어갑니다.
- **학생 화면에는 선생님용 요소를 두지 않습니다.** 버튼도 링크도 코드도 넣지 않습니다.
- 학생에게는 **자기 기록만** 보여 줍니다. 전체 명단은 선생님용 화면(teacher.html)에서만 봅니다.
- 수파베이스 표는 넣기(insert)만 열고 읽기(select)는 닫아 둡니다.
- 재원생 명단(`students`)에는 연락처가 들어갑니다. 이 표도 읽기를 완전히 닫아 두고,
  `admin_students` 함수로만 꺼냅니다.
- 같은 학생 판단 기준은 **이름(띄어쓰기 무시) + 학부모 연락처 뒷 4자리** 입니다.
  학교는 표기가 달라질 수 있어 기준으로 쓰지 않습니다.
- **재원생 명단의 기준은 이코딩(출결 시스템)입니다.** 구글 시트의 재원생 탭은 더 이상 기준이 아닙니다.
  선생님 화면의 `재원생 명단 맞추기` 에서 이코딩 학생 명단 파일을 올리면 맞춰집니다.
  **바꾸기 전에 무엇이 달라지는지 표로 먼저 보여 줍니다.** (`admin_preview_students`)
  파일 읽는 법·반 이름 표는 `이코딩-명단-맞추기.md` 와 `teacher.js` 의 `CLASS_MAP` 에 있습니다.
  **학생 줄을 지우지 않습니다.** `active = false` 로 내리고 시험 기록은 보존합니다.
- 전체 기록은 `admin_attempts` 함수로만 읽습니다.
  **선생님 비밀번호를 앱 파일에 적지 않습니다.** 서버에서 확인하게 합니다.
  (앱에 적으면 학생이 파일을 열어 볼 수 있습니다)
- `localStorage` 를 쓸 수 없는 브라우저에서도 앱이 멈추지 않아야 합니다.
